[pytest]
# Test discovery
testpaths = Tests
python_files = *.py
python_classes = Test*
python_functions = test_*

# Playwright options
addopts =
    -v
    -s
    --headed
    --browser=chromium
    --screenshot=only-on-failure
    --video=retain-on-failure